/**
 * Props interface for AnalogClock component
 */

export interface IAnalogClockProps {
  /** Optional timezone (e.g. 'America/New_York'). Uses local time if not provided. */
  timezone?: string;
}
